
# # http://wiki.linuxquestions.org/wiki/XF86_keyboard_symbols

# # https://wiki.archlinux.org/index.php/Xbindkeys

bindsym --to-code XF86AudioForward exec notify-send forward
bindsym --to-code XF86AudioRewind  exec notify-send rewind

bindsym --to-code XF86AudioLowerVolume exec notify-send lower-volume
bindsym --to-code XF86AudioRaiseVolume exec notify-send raise-volume

bindsym --to-code XF86AudioMicMute exec notify-send mic-mute
bindsym --to-code XF86AudioMute    exec notify-send mute

bindsym --to-code XF86AudioNext exec notify-send next
bindsym --to-code XF86AudioPrev exec notify-send prev

bindsym --to-code XF86AudioPlay exec notify-send play
bindsym --to-code XF86AudioStop exec notify-send stop

# bindsym XF86AudioLowerVolume exec amixer set Master 1-
# bindsym XF86AudioRaiseVolume exec amixer set Master 1+

# bindsym XF86AudioMicMute exec amixer set Speaker toggle
# bindsym XF86AudioMute    exec amixer set Master  toggle

# bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -1db && $-pulsemixer-volume > $-path-mako-pulsemixer
# bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +1db && $-pulsemixer-volume > $-path-mako-pulsemixer

# bindsym XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SINK@ toggle
# bindsym XF86AudioMute    exec pactl set-sink-mute   @DEFAULT_SINK@ toggle

### pactl

bindsym --whole-window $-key+$-mouse-m+$-alt exec pactl set-sink-volume @DEFAULT_SINK@ 0db && $-pulsemixer-volume > $-path-mako-pulsemixer

bindsym --whole-window $-key+$-mouse-u+$-alt exec pactl set-sink-volume @DEFAULT_SINK@ +1db && $-pulsemixer-volume > $-path-mako-pulsemixer
bindsym --whole-window $-key+$-mouse-d+$-alt exec pactl set-sink-volume @DEFAULT_SINK@ -1db && $-pulsemixer-volume > $-path-mako-pulsemixer

bindsym --whole-window $-key+$-mouse-u+$-alt+ctrl exec pactl set-source-mute @DEFAULT_SINK@ off && $-pulsemixer-volume > $-path-mako-pulsemixer
bindsym --whole-window $-key+$-mouse-d+$-alt+ctrl exec pactl set-source-mute @DEFAULT_SINK@ on  && $-pulsemixer-volume > $-path-mako-pulsemixer

bindsym --whole-window $-key+$-mouse-u+$-alt+ctrl+shift exec pactl set-sink-mute @DEFAULT_SINK@ off && $-pulsemixer-volume > $-path-mako-pulsemixer
bindsym --whole-window $-key+$-mouse-d+$-alt+ctrl+shift exec pactl set-sink-mute @DEFAULT_SINK@ on  && $-pulsemixer-volume > $-path-mako-pulsemixer

### playerctl

bindsym --to-code $-key+f9  exec playerctl play-pause
bindsym --to-code $-key+f10 exec playerctl stop
bindsym --to-code $-key+f11 exec playerctl previous
bindsym --to-code $-key+f12 exec playerctl next
