
### makoctl

bindsym --to-code $-key+m       exec makoctl dismiss --all
bindsym --to-code $-key+m+shift exec makoctl dismiss

bindsym --to-code $-key+x       exec makoctl dismiss --all
bindsym --to-code $-key+x+shift exec makoctl dismiss
