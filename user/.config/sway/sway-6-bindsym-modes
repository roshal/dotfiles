
### binding-modes

bindsym --to-code $-key+a mode $-mode-mode
bindsym --to-code $-key+o mode $-mode-output

bindsym --to-code ctrl+$-key+a mode $-mode-audio
bindsym --to-code ctrl+$-key+b mode $-mode-bluetooth
bindsym --to-code ctrl+$-key+c mode $-mode-cursor
bindsym --to-code ctrl+$-key+k mode $-mode-kill
bindsym --to-code ctrl+$-key+l mode $-mode-lock
bindsym --to-code ctrl+$-key+m mode $-mode-move
bindsym --to-code ctrl+$-key+n mode $-mode-network
bindsym --to-code ctrl+$-key+o mode $-mode-output
bindsym --to-code ctrl+$-key+r mode $-mode-resize
bindsym --to-code ctrl+$-key+s mode $-mode-system
bindsym --to-code ctrl+$-key+v mode $-mode-vpn
bindsym --to-code ctrl+$-key+w mode $-mode-wireguard
